<div>
  <div class="bar">
    <%= link_to "Back to projects", projects_path %>
    <%= button_to "Destroy this project", @project, method: :delete %>
  </div>
  <%= render @project %>
  <div class="bar">
    <%= link_to "DB Scrapper", project_db_scrappers_path(project_id: @project.id) %>
    <%= link_to "Knowledge Queryfier", project_knowledge_queryfiers_path(project_id: @project.id) %>
    <%= link_to "Edit this project", edit_project_path(@project) %>
  </div>
</div>

<style>
  .bar{
    margin: 25px;
    display: flex;
    justify-content: space-between;
  }
</style>