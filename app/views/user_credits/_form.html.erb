<%= form_with(model: user_credit) do |form| %>
  <% if user_credit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_credit.errors.count, "error") %> prohibited this user_credit from being saved:</h2>

      <ul>
        <% user_credit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :total_credits, style: "display: block" %>
    <%= form.text_field :total_credits %>
  </div>

  <div>
    <%= form.label :used_credits, style: "display: block" %>
    <%= form.text_field :used_credits %>
  </div>

  <div>
    <%= form.label :current_balance, style: "display: block" %>
    <%= form.text_field :current_balance %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
